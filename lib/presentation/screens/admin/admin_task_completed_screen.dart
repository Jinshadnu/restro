import 'package:flutter/material.dart';

class AdminTaskCompletedScreen extends StatelessWidget {
  const AdminTaskCompletedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
